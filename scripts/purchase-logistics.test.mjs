import test from 'node:test';import assert from 'node:assert/strict';import log from '../app/purchase-logistics.js';
const request=(id,price=60,qty=2,rev='rev')=>({id,tipo:'solicitud',estado:'con_oferta',contenido:{proveedor:id,ofertas:[{id:'offer-'+id,total:price*qty,lineas:[{snapshot:{id:'a',name:'Silla',unit:'ud'},revision:rev,qty,precio:price}]}]}});
test('offers compare only the same exact revision, quantity and unit',()=>{const a=request('A'),b=request('B',50),c=request('C',50,1),d=request('D',50,2,'other');const groups=log.groups([a,b,c,d]);assert.equal(groups.length,3);assert.equal(groups[0].offers.length,2);assert.equal(groups[0].offers[1].total,100);});
test('comparison does not mutate requests or include cancelled requests and orders',()=>{const a=request('A'),b={...request('B'),estado:'cancelada'},c={...request('C'),tipo:'pedido'},before=JSON.stringify([a,b,c]);assert.equal(log.groups([a,b,c]).length,1);assert.equal(JSON.stringify([a,b,c]),before);});
test('different identifier types never compare as the same item',()=>{const a=request('A'),b=request('B');a.contenido.ofertas[0].lineas[0].snapshot.id=1;b.contenido.ofertas[0].lineas[0].snapshot.id='1';assert.equal(log.groups([a,b]).length,2);});
