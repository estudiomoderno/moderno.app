import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import vm from 'node:vm';
const c=vm.createContext({URL});vm.runInContext(fs.readFileSync(new URL('../app/client-portal-site.js',import.meta.url),'utf8')+';this.portal=ClientPortalSite',c);
test('portal links only permit web destinations without credentials',()=>{for(const url of ['javascript:alert(1)','data:text/html,test','file:///etc/passwd','https://user:pass@example.com','bad'])assert.equal(c.portal.linkURL(url),'');assert.equal(c.portal.linkURL('https://example.com/tour'),'https://example.com/tour');});
test('portal links hide private entries and escape user labels',()=>{const html=c.portal.linksHTML({files:[{docKind:'portal-link',cli:true,name:'Tour <360>',url:'https://example.com/tour'},{docKind:'portal-link',cli:false,name:'Private',url:'https://example.com/private'}]});assert.match(html,/Tour &lt;360&gt;/);assert.match(html,/noopener noreferrer/);assert.doesNotMatch(html,/Private|private/);});
